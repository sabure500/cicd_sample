name: Gradle test

on: [push]

jobs:
  gradle-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: gradle test
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
        run: |
          ls -l
          echo $PWD
          java -version